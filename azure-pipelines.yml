trigger:
- main

pool: Default

variables:
  imageName: 'omardiablo/auredevops'
  tag: '$(Build.BuildId)'
  Build Sources Directory: $(Build.SourcesDirectory)

steps:

- task: Docker@2
  inputs:
    containerRegistry: 'dockerhub-connection'
    repository: '$(imageName)'
    command: 'buildAndPush'
    Dockerfile: '$(Build.SourcesDirectory)/Dockerfile'   # If Dockerfile is at repo root
    buildContext: '$(Build.SourcesDirectory)'            # Build context = repo root
    tags: |
      latest
      $(tag)